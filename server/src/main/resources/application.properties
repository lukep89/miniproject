
## ===== LOCAL =====
## ===== SQL =====
# spring.datasource.url=jdbc:mysql://localhost:3306/cafeapp
# spring.datasource.username=luke
# spring.datasource.password=${SQL_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

## ===== MONGO =====
# mongo.url=mongodb://localhost:27017


## ===== FOR SENDING EMAIL =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# linkForResetPassword=http://localhost:4200
# # linkForResetPassword=http://localhost:8080
linkForResetPassword=cafemgmtapp-production.up.railway.app

## ===== JWT =====
jwtSecretKey=${JWT_SECRET_KEY}


## ===== RAILWAY =====
## Railway: URL needs 'jdbc:' infront'
spring.datasource.url=${MYSQL_URL} 
spring.datasource.username=${MYSQLUSER} 
spring.datasource.password=${MYSQLPASSWORD} 
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

spring.data.mongodb.url=${MONGO_URL}
spring.data.mongodb.host=${MONGOHOST}
spring.data.mongodb.port=${MONGOPORT}
spring.data.mongodb.username=${MONGOUSER}
spring.data.mongodb.password=${MONGOPASSWORD}
spring.data.mongodb.database=${MONGODB}
spring.data.mongodb.authentication-database=admin

